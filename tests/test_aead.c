#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <time.h>
#include "aead.h"
#include "xchacha20.h"

#define REP32(x) x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x
#define REP24(x) x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x

// ==================== 测试数据 ====================
static const uint8_t k1[32] = { REP32(0x01) };
static const uint8_t n1[24] = { REP24(0x02) };
static const uint8_t tg1[16] = {
    0x82,0x7d,0xf5,0x41,0x04,0x36,0x84,0x93,
    0x08,0x17,0x73,0x4c,0x9b,0xa9,0x0b,0xa5
};

static const uint8_t k2[32] = { REP32(0x11) };
static const uint8_t n2[24] = { REP24(0x22) };
static const uint8_t pt2[16] = {
    0x00,0x01,0x02,0x03,0x04,0x05,0x06,0x07,
    0x08,0x09,0x0a,0x0b,0x0c,0x0d,0x0e,0x0f
};
static const uint8_t ct2[16] = {
    0x83,0x66,0xe5,0x0f,0x58,0xbc,0xe2,0x93,
    0xed,0x44,0x1e,0xc0,0xb1,0x74,0xaf,0xed
};
static const uint8_t tg2[16] = {
    0xc4,0x1c,0xd3,0x0f,0x14,0x41,0x33,0x08,
    0x52,0x38,0xa4,0x52,0xc0,0xc1,0xcb,0x27
};

static const uint8_t k3[32] = { REP32(0x33) };
static const uint8_t n3[24] = { REP24(0x44) };
static const uint8_t pt3[64] = {
    0x00,0x01,0x02,0x03,0x04,0x05,0x06,0x07,
    0x08,0x09,0x0a,0x0b,0x0c,0x0d,0x0e,0x0f,
    0x10,0x11,0x12,0x13,0x14,0x15,0x16,0x17,
    0x18,0x19,0x1a,0x1b,0x1c,0x1d,0x1e,0x1f,
    0x20,0x21,0x22,0x23,0x24,0x25,0x26,0x27,
    0x28,0x29,0x2a,0x2b,0x2c,0x2d,0x2e,0x2f,
    0x30,0x31,0x32,0x33,0x34,0x35,0x36,0x37,
    0x38,0x39,0x3a,0x3b,0x3c,0x3d,0x3e,0x3f
};
static const uint8_t ct3[64] = {
    0x18,0xa7,0x72,0xd7,0x34,0xe7,0x31,0x84,
    0x9a,0x79,0x41,0x15,0x1c,0xda,0xcc,0xa8,
    0x2e,0x13,0xf0,0x2b,0x34,0xc7,0x51,0x56,
    0x1d,0xa9,0xcd,0x48,0xae,0x1a,0x58,0x1c,
    0xf4,0x90,0x69,0xff,0xbd,0x40,0x7a,0xbb,
    0xb3,0xf5,0x27,0xf4,0xff,0x85,0xc1,0x01,
    0xe1,0x29,0x4a,0xd0,0xd6,0x68,0x4d,0x55,
    0xa0,0xdc,0x66,0x67,0xb4,0xb3,0x7c,0xbe
};
static const uint8_t tg3[16] = {
    0x05,0xbb,0x34,0x0e,0xa9,0x1b,0x28,0x0d,
    0x68,0xd5,0x62,0x62,0x86,0xab,0x09,0x45
};

static const uint8_t k4[32] = { REP32(0x55) };
static const uint8_t n4[24] = { REP24(0x66) };
static const uint8_t tg4[16] = {
    0xe1,0x88,0x1f,0xc0,0x6a,0xef,0xe4,0xcf,
    0x96,0x94,0x35,0xde,0xb6,0x85,0x81,0x45
};
static const uint8_t ct4_expected[256] = {
    
};

static const uint8_t k5[32] = { REP32(0x77) };
static const uint8_t n5[24] = { REP24(0x88) };
static const uint8_t aad5[32] = { REP32(0xAA) };
static const uint8_t pt5[64] = {
    0x0d,0x14,0x1b,0x22,0x29,0x30,0x37,0x3e,
    0x45,0x4c,0x53,0x5a,0x61,0x68,0x6f,0x76,
    0x7d,0x84,0x8b,0x92,0x99,0xa0,0xa7,0xae,
    0xb5,0xbc,0xc3,0xca,0xd1,0xd8,0xdf,0xe6,
    0xed,0xf4,0xfb,0x02,0x09,0x10,0x17,0x1e,
    0x25,0x2c,0x33,0x3a,0x41,0x48,0x4f,0x56,
    0x5d,0x64,0x6b,0x72,0x79,0x80,0x87,0x8e,
    0x95,0x9c,0xa3,0xaa,0xb1,0xb8,0xbf,0xc6
};
static const uint8_t ct5[64] = {
    0x98,0x09,0xa9,0x0f,0x46,0x62,0xb4,0x33,
    0xc7,0x9e,0x18,0x95,0x6f,0xea,0x4a,0xde,
    0xd9,0xe8,0xf0,0xbb,0x5d,0x1c,0x64,0x38,
    0x33,0x75,0x8f,0x3a,0x2d,0x88,0x92,0x8c,
    0x0b,0xe3,0xd4,0x3a,0x10,0x75,0x5e,0x5a,
    0xca,0xc9,0x78,0x68,0x0f,0xaf,0x2a,0x07,
    0x60,0x65,0x39,0xe2,0x38,0x14,0xa5,0xa3,
    0xd5,0xde,0xbe,0xda,0xec,0x5f,0x79,0x3c
};
static const uint8_t tg5[16] = {
    0xee,0xec,0xc6,0xa9,0x14,0xeb,0x35,0x75,
    0x6f,0xf4,0xc9,0x92,0xd8,0xd5,0xd7,0x74
};

// ==================== 打印函数 ====================
static void print_hex(const char *label, const uint8_t *data, size_t len, size_t max) {
    printf("%s: ", label);
    size_t n = (len < max) ? len : max;
    for (size_t i = 0; i < n; i++) printf("%02x", data[i]);
    if (len > max) printf("...");
    printf("\n");
}

// ==================== 测试函数 ====================
static void test_encrypt(const char *name,
                         const uint8_t *key, const uint8_t *nonce,
                         const uint8_t *aad, size_t aad_len,
                         const uint8_t *plain, size_t plen,
                         const uint8_t *exp_ct, const uint8_t *exp_tag,
                         size_t outcipher_len) {
    uint8_t ciphertext[1024] = {0};
    uint8_t tag[16] = {0};
    uint8_t decrypted[1024] = {0};

    printf("\n=== %s ===\n", name);
    printf("plaintext_len=%zu\n", plen);

    xchacha20_poly1305_encrypt(key, nonce, aad, aad_len, plain, plen, ciphertext, tag);

    print_hex("  exp_ct", exp_ct, plen, outcipher_len);
    print_hex("  got_ct", ciphertext, plen, outcipher_len);
    print_hex("  exp_tag", exp_tag, 16, 16);
    print_hex("  got_tag", tag, 16, 16);

    if (plen > 0 && memcmp(ciphertext, exp_ct, plen) != 0) {
        printf("  [FAIL] 密文不匹配\n");
        return;
    }
    if (memcmp(tag, exp_tag, 16) != 0) {
        printf("  [FAIL] 标签不匹配\n");
        return;
    }
    printf("  [OK] 加密通过\n");

    int ret = xchacha20_poly1305_decrypt(key, nonce, aad, aad_len, ciphertext, plen, tag, decrypted);
    if (ret != 0 || memcmp(decrypted, plain, plen) != 0) {
        printf("  [FAIL] 解密失败\n");
        return;
    }
    printf("  [OK] 解密通过\n");
}

// ==================== 性能测试 ====================
#ifdef _WIN32
    #include <windows.h>
    static double wall_time(void) {
        LARGE_INTEGER freq, t;
        QueryPerformanceFrequency(&freq);
        QueryPerformanceCounter(&t);
        return (double)t.QuadPart / (double)freq.QuadPart;
    }
#else
    #include <sys/time.h>
    static double wall_time(void) {
        struct timeval tv;
        gettimeofday(&tv, NULL);
        return (double)tv.tv_sec + (double)tv.tv_usec / 1e6;
    }
#endif

static void bench_aead(const char *name, size_t size, int iterations) {
    uint8_t key[32] = {0x01};
    uint8_t nonce[24] = {0x02};
    uint8_t *plain = malloc(size);
    uint8_t *cipher = malloc(size);
    uint8_t tag[16];

    if (!plain || !cipher) {
        free(plain);
        free(cipher);
        printf("%s: 内存分配失败\n", name);
        return;
    }

    memset(plain, 0xAA, size);

    // 预热
    xchacha20_poly1305_encrypt(key, nonce, NULL, 0, plain, size, cipher, tag);

    double start = wall_time();
    for (int i = 0; i < iterations; i++) {
        xchacha20_poly1305_encrypt(key, nonce, NULL, 0, plain, size, cipher, tag);
    }
    double elapsed = wall_time() - start;

    double mbps = (double)size * iterations / (1024.0 * 1024.0) / elapsed;
    printf("%-20s size=%-10zu iter=%-5d %.2f MB/s\n",
           name, size, iterations, mbps);

    free(plain);
    free(cipher);
}

static void bench_xchacha20(const char *name, size_t size, int iterations) {
    uint8_t key[32] = {0x01};
    uint8_t nonce[24] = {0x02};
    uint8_t *in = malloc(size);
    uint8_t *out = malloc(size);
    xchacha20_ctx ctx;

    if (!in || !out) {
        free(in);
        free(out);
        printf("%s: 内存分配失败\n", name);
        return;
    }

    memset(in, 0xAA, size);

    xchacha20_init(&ctx, key, nonce, 0);
    xchacha20_crypt(&ctx, in, out, size);

    double start = wall_time();
    for (int i = 0; i < iterations; i++) {
        xchacha20_init(&ctx, key, nonce, 0);
        xchacha20_crypt(&ctx, in, out, size);
    }
    double elapsed = wall_time() - start;

    double mbps = (double)size * iterations / (1024.0 * 1024.0) / elapsed;
    printf("%-20s size=%-10zu iter=%-5d %.2f MB/s\n",
           name, size, iterations, mbps);

    free(in);
    free(out);
}

// 在 main 函数末尾调用，或者单独加一个性能测试入口
static void run_benchmarks(void) {
    printf("\n=== 性能测试 ===\n");
    bench_aead("AEAD 1KB", 1024, 1000);
    bench_aead("AEAD 4KB", 4096, 500);
    bench_aead("AEAD 16KB", 16384, 200);
    bench_aead("AEAD 64KB", 65536, 50);
    bench_aead("AEAD 32MB", 32*1024*1024, 10);
    bench_aead("AEAD 4MB", 64*1024*1024, 5);
    bench_xchacha20("XChaCha20 1KB", 1024, 1000);
    bench_xchacha20("XChaCha20 4KB", 4096, 500);
    bench_xchacha20("XChaCha20 16KB", 16384, 200);
    bench_xchacha20("XChaCha20 64KB", 65536, 50);
    bench_xchacha20("XChaCha20 32MB", 32*1024*1024, 10);
    bench_xchacha20("XChaCha20 64MB", 64*1024*1024, 5);
}

// ==================== 主函数 ====================
int main(void) {
    printf("=== XChaCha20-Poly1305 测试 ===\n");

    // 向量4 明文
    static uint8_t pt4[256];
    for (int i = 0; i < 256; i++) pt4[i] = (uint8_t)(0x0d + i * 7);

    test_encrypt("TEST1 (空明文)", k1, n1, NULL, 0, NULL, 0, NULL, tg1, 16);
    test_encrypt("TEST2 (16字节)", k2, n2, NULL, 0, pt2, 16, ct2, tg2, 16);
    test_encrypt("TEST3 (64字节)", k3, n3, NULL, 0, pt3, 64, ct3, tg3, 64);
    test_encrypt("TEST4 (256字节)", k4, n4, NULL, 0, pt4, 256, ct4_expected, tg4, 256);
    test_encrypt("TEST5 (带AAD 32字节，明文64字节)", k5, n5, aad5, 32, pt5, 64, ct5, tg5, 64);
    run_benchmarks();
    printf("\n=== 测试完成 ===\n");
    return 0;
}